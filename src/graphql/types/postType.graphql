type Post {
  id: ID!
  # title: String
  # description: String
  content: String
  User: User!
  # likeCount: Int!
  Likers: [User!]!
  # viewCount: Int!
  category: String
  # PostComments: [Comment!]!
  # commentCount: Int!
  # thumbnail: String
  createdAt: String!
  updatedAt: String!
}

type PostList {
  rows: [Post!]!
  count: Int!
}

type Query {
  posts(
    offset: Int
    limit: Int!
    order: String
    searchKeyword: String
    category: String
    userId: String
    isDev: Boolean # isThereThumb: Boolean
  ): PostList!

  post(id: String!): Post!
}

type Mutation {
  addPost(
    # title: String
    # description: String
    content: String
    category: String
    # thumbnail: String
    isDev: Boolean
  ): Boolean!

  updatePost(
    id: String!
    # title: String
    # description: String
    content: String
    category: String
    # thumbnail: String
    isDev: Boolean
  ): Boolean!

  deletePost(id: String!, isDev: Boolean): Boolean!

  likePost(id: String!, isDev: Boolean): Boolean!

  unlikePost(id: String!, isDev: Boolean): Boolean!
}
