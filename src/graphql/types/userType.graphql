type User {
  id: ID!
  nickname: String!
  email: String!
  isMaster: Boolean!
  avatar: String
  Posts: [Post!]!
  # likes: [Like!]!
  # UserComments: [Comment!]!
  PostCount: Int
  token: String
  createdAt: String!
  updatedAt: String!
}

type UserList {
  rows: [User!]!
  count: Int!
}

type Query {
  users(offset: Int, limit: Int!, order: String): UserList!

  recommenders(offset: Int, limit: Int!): [User!]!

  user(id: String!): User!

  me: User!
}

type Mutation {
  logIn(email: String!, password: String!): User!

  logInWithGoogle(email: String!, nickname: String!): User!

  addUser(
    email: String!
    password: String!
    nickname: String!
    avatar: String
  ): Boolean!

  updateUser(password: String, nickname: String, avatar: String): User!
}
