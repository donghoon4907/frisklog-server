type Notice {
  id: ID!
  title: String!
  description: String!
  createdAt: String!
  updatedAt: String!
  deletedAt: String!
}

# type Query {
#   # posts: [Post]
# }

# type Mutation {
#   addPost(title: String!, description: String): Post
# }
